# Production SSH credentials (example)
# Copy to credentials.yaml and fill real usernames/passwords.
credentials:
  REPLACE_SERIAL:
    username: admin
    password: change_me

default:
  username: admin
  password: change_me
